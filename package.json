{
  "name": "crypto-hogs",
  "version": "1.0.0",
  "main": "./dist/main.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn run build  && electron .",
    "build": "tsc && tailwindcss -i src/styles/style.css -o dist/styles/output.css"
  },
  "devDependencies": {
    "electron": "^29.1.0",
    "tailwindcss": "^3.4.1"
  },
  "dependencies": {
    "@types/electron": "^1.6.10",
    "electron-reload": "^2.0.0-alpha.1"
  }
}
